# Pinning the Rust toolchain so every contributor and CI runs the same
# compiler. Bump this when a workspace dependency requires a newer rustc.
[toolchain]
channel = "1.90.0"
components = ["rustfmt", "clippy"]
targets = ["aarch64-unknown-linux-gnu"]
profile = "minimal"
